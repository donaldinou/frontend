{% set no_padding_middle = no_padding_middle|default(false) %}
<div class="container">
    <div class="row">
        <div class="col-md-12{% if no_padding_middle is same as(true) %} no-padding{% endif %}">
            {% block middle %}

            {% endblock %}
        </div>
    </div>
</div>
