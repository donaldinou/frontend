/*if (requirejs) {
    requirejs.config({

    })
}*/

if (window.adsbygoogle != null) {
    setTimeout(function() {
        return jQuery('.ad_container').each(function() {
            return window.adsbygoogle.push({});
        });
    }, 150);
}
