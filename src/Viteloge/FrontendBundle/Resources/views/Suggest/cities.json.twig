{%- if cities -%}
{"results":[{%- for key, city in cities -%}
        {"id":"{{ city.id }}","text":"{{ city.name|replace({"'": "-"}, {'"': ''}) ~ ' (' ~ city.postalcode ~ ')'}}"}{%- if not loop.last -%},{%- endif -%}
    {%- endfor -%}]}
{%- else-%}
{"results":[]}
{%- endif -%}
