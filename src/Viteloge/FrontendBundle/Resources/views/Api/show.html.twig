{%- if includeLibs is not defined -%}
    {%- set includeLibs = true -%}{# |default(true) is not an option because it will set always true #}
{%- endif -%}
{%- if includeLibs is same as(true) -%}
    <link rel="stylesheet" href="{{ aws_s3_domain('built/viteloge.min.css') }}">
{%- endif -%}
{{- include('VitelogeFrontendBundle:Api:form/adSearch.html.twig', { form: form, theme_container: 'search-container', includeLibs: includeLibs }) -}}

{#

New way to implement

window.viteloge = (function(d, s, id) {
    var js, fjs = d.getElementsByTagName(s)[0],
        v = window.viteloge || {};
    if (d.getElementById(id)) return v;
    js = d.createElement(s);
    js.id = id;
    js.src = "http://www.viteloge.com/sdk.js";
    fjs.parentNode.insertBefore(js, fjs);
}(document, "script", "viteloge-jssdk"));

#}
