<script src="{{ asset('assets/js/require.js') }}"></script>
<script>
    requirejs.config({
        baseUrl: "{{ asset('bundles/frontendbundle/js') }}"
    })
    require(['viteloge'], function(common) {
        require(['{{ module }}']); // load {module}.js
    });
</script>
