<figure class="col-sm-6 col-md-3">
    <div class="thumbnail">
        <div class="center-block">
            <a href="{{ ad.link }}{#{{ path('viteloge_frontend_ad_redirect', { 'id': ad.id }) }}#}">
                <img src="{{ ad.image }}{#{{ 'http://img.viteloge.com/' ~ ad.photo }}#}" class="media-object" alt="image">
            </a>
        </div>
        <figcaption class="caption">
            {% if ad.price is defined %}
                <span class="price">{{ ad.price|replace('EUR', '€') }}{#{{ ad.price | localizedcurrency('EUR') }}#}</span>
            {% endif %}
        </figcaption>
    </div>
</figure>
